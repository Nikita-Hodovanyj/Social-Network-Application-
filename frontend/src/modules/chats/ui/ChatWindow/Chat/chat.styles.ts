import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    chatContainer: {
        padding: 10,
    },
    sameSenderMessages: {
        alignItems: "flex-start",
        padding: 5,
        gap: 10,
        marginBottom: 10
    },
    myMessages: {
        alignItems: "flex-end"
    }
})